/// <reference types="vite/client" />

// The git commit the app was built from, injected by Vite's `define`
// (vite.config.ts). Used to reset saved scores on a new deploy.
declare const __APP_VERSION__: string;
